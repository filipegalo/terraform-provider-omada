---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "omada_dhcp_reservation Resource - omada"
subcategory: ""
description: |-
  A DHCP static IP reservation on an Omada Controller site.
---

# omada_dhcp_reservation (Resource)

A DHCP static IP reservation on an Omada Controller site.

## Example Usage

```terraform
resource "omada_dhcp_reservation" "printer" {
  # site_id defaults to the provider's configured site; set it explicitly only
  # to target a different site from this provider instance.
  network_id  = "6142yyyyyyyyyyyyyyyyyyyy"
  mac_address = "AA:BB:CC:DD:EE:FF"
  ip_address  = "192.168.1.50"
  description = "Office printer"
  enabled     = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `ip_address` (String) IP address to reserve for the device.
- `mac_address` (String) MAC address of the device to reserve an IP for. Together with site_id, this is the reservation's real identity.
- `network_id` (String) ID of the LAN network the reservation's IP address belongs to. Not updatable in place: the DHCP reservation API only patches ip/description/status.

### Optional

- `description` (String) Free-text description of the reservation.
- `enabled` (Boolean) Whether the reservation is active.
- `site_id` (String) ID of the site this reservation belongs to. Defaults to the provider's configured site; set explicitly to target a different site from this provider instance.

### Read-Only

- `id` (String) Synthesized as "{site_id}:{mac_address}".

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Reservations are imported as "<site>:<mac_address>", where <site> is either
# the site ID or the site name. The MAC may use either separator.
terraform import omada_dhcp_reservation.printer Default:AA:BB:CC:DD:EE:FF
```
